# BrainSpark 环境变量示例
# 复制为 .env.local 后填入实际值（.env.local 已被 gitignore，不会提交）

# 应用名称（显示在标题栏）
VITE_APP_NAME=BrainSpark

# Claude API 默认 Base URL（留空则用官方 https://api.anthropic.com）
# 国内代理示例：https://your-proxy.example.com
VITE_CLAUDE_BASE_URL=

# 日志服务地址（本地开发用，默认 http://127.0.0.1:5174）
# 设为空字符串则禁用日志上报
VITE_LOG_SERVER=http://127.0.0.1:5174
